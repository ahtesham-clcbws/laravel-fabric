<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" data-theme="light">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ config('app.name', 'Laravel') }} - DaisyUI Minimal</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @livewireStyles
</head>
<body class="bg-base-100 min-h-screen">
    <div class="max-w-4xl mx-auto px-4 py-20">
        <header class="flex justify-between items-center mb-16">
            <h1 class="text-3xl font-black uppercase tracking-tighter text-primary">Fabric</h1>
            <nav class="flex gap-4">
                <a href="#" class="btn btn-ghost btn-sm">Overview</a>
                <a href="#" class="btn btn-ghost btn-sm">Settings</a>
            </nav>
        </header>

        <main class="prose prose-lg max-w-none">
            {{ $slot }}
        </main>

        <footer class="mt-32 pt-8 border-t border-base-200 text-center opacity-30 text-xs">
            &copy; {{ date('Y') }} Fabricated for the web.
        </footer>
    </div>
    @livewireScripts
</body>
</html>
