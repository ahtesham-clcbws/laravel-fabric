<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" data-theme="{{ config('fabric.theme_variant', 'light') }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ $title ?? config('app.name', 'Laravel') }} - Admin</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @livewireStyles
</head>
<body class="font-sans antialiased text-base-content bg-base-200">
    <div class="min-h-screen flex flex-col">
        <!-- Top Navbar -->
        <div class="navbar bg-base-100 border-b border-base-300 px-4 lg:px-8 sticky top-0 z-50">
            <div class="navbar-start">
                <a href="{{ route('dashboard') }}" class="flex items-center gap-3 group">
                    <div class="w-8 h-8 bg-primary text-primary-content flex items-center justify-center rounded-lg font-black transition group-hover:rotate-12">F</div>
                    <span class="text-xl font-black uppercase tracking-tighter hidden sm:inline-block">{{ config('app.name') }}</span>
                </a>
            </div>
            
            <div class="navbar-center">
                <livewire:fabric.omnisearch />
            </div>

            <div class="navbar-end gap-2">
                <ul class="menu menu-horizontal px-1 hidden lg:flex gap-1 mr-4">
                    <li><a href="{{ route('dashboard') }}" @class(['active font-bold' => request()->routeIs('dashboard')]) wire:navigate>{{ __('Dashboard') }}</a></li>
                </ul>

                <div class="dropdown dropdown-end">
                    <div tabindex="0" role="button" class="btn btn-ghost btn-circle avatar border-2 border-primary">
                        <div class="w-10 rounded-full">
                            <img src="https://ui-avatars.com/api/?name={{ urlencode(auth()->user()->name) }}&color=7F9CF5&background=EBF4FF" />
                        </div>
                    </div>
                    <ul tabindex="0" class="mt-3 z-[1] p-2 shadow-2xl menu menu-sm dropdown-content bg-base-100 rounded-box w-52 border border-base-300">
                        <li><a href="{{ route('profile') }}" wire:navigate>{{ __('Profile') }}</a></li>
                        <li>
                            <form method="POST" action="{{ route('logout') }}" class="w-full">
                                @csrf
                                <button type="submit" class="w-full text-left text-error">{{ __('Logout') }}</button>
                            </form>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Page Content -->
        <main class="p-4 sm:p-8 flex-1">
            <div class="max-w-7xl mx-auto">
                {{ $slot }}
            </div>
        </main>
    </div>
    @livewireScripts
</body>
</html>
