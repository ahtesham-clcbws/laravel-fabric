<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ config('app.name', 'Laravel') }} - Float UI Minimal</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @livewireStyles
</head>
<body class="bg-gray-50/50">
    <div class="max-w-screen-md mx-auto px-4 py-24">
        <header class="flex justify-between items-center mb-16">
            <h1 class="text-{{ PRIMARY }} font-bold text-xl tracking-tighter">FABRIC</h1>
            <nav class="flex gap-x-6 text-sm font-medium text-gray-500">
                <a href="#" class="hover:text-{{ PRIMARY }} transition">Overview</a>
            </nav>
        </header>

        <main>
            {{ $slot }}
        </main>
    </div>
    @livewireScripts
</body>
</html>
