<?php

namespace {{ NAMESPACE }}\Traits\Ghost;

use {{ NAMESPACE }}\Traits\Audit\Auditable;
use {{ NAMESPACE }}\Traits\Media\InteractsWithMedia;
use {{ NAMESPACE }}\Traits\Cortex\Cacheable;
use {{ NAMESPACE }}\Traits\Tenancy\BelongsToTenant;

trait Ghostable
{
    use Auditable, InteractsWithMedia, Cacheable, BelongsToTenant;

    /**
     * The Master Ghost Trait.
     * This trait bridges all native Fabric plugins into a single injection.
     */
}
