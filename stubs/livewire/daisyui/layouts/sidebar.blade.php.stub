<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" x-data="{ theme: localStorage.getItem('fabric-theme') || (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light') }" :data-theme="theme">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ $title ?? config('app.name', 'Laravel') }} - Admin</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @livewireStyles
</head>
<body class="font-sans antialiased text-base-content bg-base-200">
    <div class="drawer lg:drawer-open">
        <input id="main-drawer" type="checkbox" class="drawer-toggle" />
        
        <div class="drawer-content flex flex-col">
            <!-- Mobile Navbar -->
            <div class="navbar bg-base-100 border-b border-base-300 lg:hidden px-4">
                <div class="flex-none">
                    <label for="main-drawer" class="btn btn-square btn-ghost drawer-button">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="inline-block w-6 h-6 stroke-current"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
                    </label>
                </div>
                <div class="flex-1 font-bold text-xl uppercase tracking-tighter">{{ config('app.name') }}</div>
                <div class="flex-none">
                    <livewire:fabric.omnisearch />
                </div>
            </div>

            <!-- Page Content -->
            <main class="p-4 sm:p-8 min-h-screen">
                @if(session()->has('fabric_impersonator_id'))
                    <div class="alert alert-warning shadow-lg mb-6 rounded-xl border border-warning/20">
                        <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" /></svg>
                        <div class="flex-1">
                            <h3 class="font-bold uppercase text-xs tracking-widest">Impersonation Mode</h3>
                            <div class="text-xs opacity-75">You are currently logged in as <strong>{{ auth()->user()->name }}</strong>.</div>
                        </div>
                        <div class="flex-none">
                            <form action="{{ route('fabric.impersonate.leave') }}" method="POST">
                                @csrf
                                <button type="submit" class="btn btn-sm btn-ghost border-warning/50">Return to Admin</button>
                            </form>
                        </div>
                    </div>
                @endif

                <div class="max-w-7xl mx-auto">
                    {{ $slot }}
                </div>
            </main>
        </div>

        <!-- Sidebar -->
        <div class="drawer-side z-50">
            <label for="main-drawer" class="drawer-overlay"></label>
            <div class="flex flex-col w-80 min-h-full bg-base-100 text-base-content border-r border-base-300">
                
                <!-- Sidebar Header -->
                <div class="p-6">
                    <a href="/" class="flex items-center gap-3 mb-6 group">
                        <div class="w-10 h-10 bg-primary text-primary-content flex items-center justify-center rounded-xl font-black text-xl shadow-lg transition group-hover:rotate-12">F</div>
                        <span class="text-2xl font-black uppercase tracking-tighter">{{ config('app.name') }}</span>
                    </a>
                    
                    <!-- Spotlight Search -->
                    <livewire:fabric.omnisearch />
                </div>

                <!-- Navigation -->
                <ul class="menu p-4 pt-0 gap-1 flex-1">
                    <li class="menu-title opacity-40 uppercase text-xs font-bold">{{ __('General') }}</li>
                    <li>
                        <a href="{{ route('dashboard') }}" @class(['active font-bold' => request()->routeIs('dashboard')]) wire:navigate>
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" /></svg>
                            {{ __('Dashboard') }}
                        </a>
                    </li>

                    @php
                        $registry = app(\CLCBWS\Fabric\Services\SearchRegistry::class);
                        $groupedResources = collect($registry->getResources())->groupBy('group');
                    @endphp

                    @foreach($groupedResources as $group => $resources)
                        <li class="menu-title opacity-40 uppercase text-xs font-bold mt-4">{{ __($group) }}</li>
                        @foreach($resources as $name => $res)
                            <li>
                                <a href="{{ route($res['route']) }}" @class(['active font-bold' => request()->routeIs($res['route'])]) wire:navigate>
                                    <span class="text-xl">{{ $res['icon'] }}</span>
                                    {{ \Illuminate\Support\Str::plural(\Illuminate\Support\Str::headline($name)) }}
                                </a>
                            </li>
                        @endforeach
                    @endforeach
                </ul>

                <!-- Sidebar Footer (User Profile) -->
                <div class="p-4 border-t border-base-300 bg-base-200/50">
                    <div class="flex items-center gap-3 px-2 py-3 rounded-xl hover:bg-base-300 transition cursor-pointer group">
                        <div class="avatar online">
                            <div class="w-10 rounded-full border-2 border-primary">
                                <img src="https://ui-avatars.com/api/?name={{ urlencode(auth()->user()->name) }}&color=7F9CF5&background=EBF4FF" />
                            </div>
                        </div>
                        <div class="flex-1 overflow-hidden">
                            <div class="font-bold truncate">{{ auth()->user()->name }}</div>
                            <div class="text-xs opacity-50 truncate">{{ auth()->user()->email }}</div>
                        </div>
                        
                        <div class="dropdown dropdown-top dropdown-end">
                            <div tabindex="0" role="button" class="btn btn-ghost btn-xs btn-circle">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v.01M12 12v.01M12 19v.01M12 6a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2z" /></svg>
                            </div>
                            <ul tabindex="0" class="dropdown-content z-[1] menu p-2 shadow-2xl bg-base-100 rounded-box w-52 border border-base-300">
                                <li class="menu-title text-xs opacity-50 uppercase tracking-widest">{{ __('Settings') }}</li>
                                <li>
                                    <button @click="theme = (theme === 'dark' ? 'light' : 'dark'); localStorage.setItem('fabric-theme', theme)">
                                        <svg x-show="theme === 'light'" xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" /></svg>
                                        <svg x-show="theme === 'dark'" xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-warning" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 9H3m3.343-5.657l.707.707m12.728 12.728l.707.707M6.343 17.657l-.707.707M17.657 6.343l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" /></svg>
                                        <span>{{ __('Toggle Theme') }}</span>
                                    </button>
                                </li>
                                <div class="divider my-0 opacity-20"></div>
                                <li><a href="{{ route('profile') }}" wire:navigate>{{ __('My Profile') }}</a></li>
                                <li>
                                    <form method="POST" action="{{ route('logout') }}" class="w-full">
                                        @csrf
                                        <button type="submit" class="w-full text-left text-error">{{ __('Logout') }}</button>
                                    </form>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    @livewireScripts
</body>
</html>
