<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" 
      x-data="{ 
        theme: localStorage.getItem('fabric-theme') || (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light') 
      }" 
      :class="theme === 'dark' ? 'dark' : ''">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>{{ $title ?? config('app.name', 'Laravel') }} - Fabric</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @livewireStyles
    <style>
        [x-cloak] { display: none !important; }
    </style>
</head>
<body class="font-sans antialiased bg-neutral-50 dark:bg-neutral-950 text-neutral-800 dark:text-neutral-200">
    <div class="min-h-screen flex flex-col">
        <!-- Top Navbar (Pure Tailwind + Alpine) -->
        <nav class="sticky top-0 z-50 bg-white/80 backdrop-blur-2xl border-b border-neutral-100 dark:bg-neutral-950/80 dark:border-neutral-900 px-8 py-5">
            <div class="max-w-7xl mx-auto flex items-center justify-between">
                <div class="flex items-center gap-x-3 group cursor-pointer">
                    <div class="size-10 rounded-xl bg-{{ PRIMARY }} flex items-center justify-center shadow-2xl shadow-{{ PRIMARY }}/30 group-hover:rotate-12 transition-all duration-500 text-white font-black">F</div>
                    <span class="text-xl font-black text-neutral-800 dark:text-white tracking-tighter uppercase hidden sm:inline-block">{{ config('app.name') }}</span>
                </div>

                <div class="flex-1 max-w-lg mx-12 hidden md:block">
                    @livewire('fabric.omnisearch')
                </div>

                <div class="flex items-center gap-6">
                    <div class="hidden lg:flex items-center gap-8 mr-6">
                        <a href="{{ route('fabric.dashboard') }}" 
                           @class([
                               'text-[10px] font-black uppercase tracking-[0.2em] transition-colors',
                               'text-{{ PRIMARY }}' => request()->routeIs('fabric.dashboard'),
                               'text-neutral-400 hover:text-neutral-600' => !request()->routeIs('fabric.dashboard')
                           ])
                           wire:navigate>{{ __('Dashboard') }}</a>
                    </div>

                    @auth
                    <div class="relative" x-data="{ userMenu: false }">
                        <button 
                            @click="userMenu = !userMenu"
                            class="flex items-center gap-3 p-1 rounded-full hover:bg-neutral-50 dark:hover:bg-neutral-800 transition-all border border-transparent hover:border-neutral-100 dark:hover:border-neutral-700"
                        >
                            <div class="size-10 rounded-full overflow-hidden shadow-lg ring-2 ring-{{ PRIMARY }}">
                                <img src="https://ui-avatars.com/api/?name={{ urlencode(auth()->user()->name) }}&color=7F9CF5&background=EBF4FF" />
                            </div>
                        </button>

                        <div 
                            x-show="userMenu" 
                            @click.away="userMenu = false"
                            x-transition
                            x-cloak
                            class="absolute top-full right-0 mt-4 w-64 bg-white dark:bg-neutral-900 rounded-[24px] border border-neutral-100 dark:border-neutral-800 shadow-2xl p-2 z-50"
                        >
                            <div class="px-4 py-3 mb-2 border-b border-neutral-50 dark:border-neutral-800">
                                <div class="text-[10px] font-black uppercase tracking-widest text-neutral-800 dark:text-white">{{ auth()->user()->name }}</div>
                                <div class="text-[8px] font-bold text-neutral-400 uppercase tracking-widest mt-1">System Master</div>
                            </div>
                            <button 
                                @click="theme = (theme === 'dark' ? 'light' : 'dark'); localStorage.setItem('fabric-theme', theme)"
                                class="w-full flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-neutral-50 dark:hover:bg-neutral-800 text-[10px] font-black uppercase tracking-widest text-neutral-500"
                            >
                                <svg x-show="theme === 'light'" class="size-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" /></svg>
                                <svg x-show="theme === 'dark'" class="size-4 text-amber-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M12 3v1m0 16v1m9-9h-1M4 9H3m3.343-5.657l.707.707m12.728 12.728l.707.707M6.343 17.657l-.707.707M17.657 6.343l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" /></svg>
                                <span>{{ __('Toggle Theme') }}</span>
                            </button>
                            <div class="h-px bg-neutral-100 dark:bg-neutral-800 my-1"></div>
                            <a href="{{ route('fabric.profile') }}" class="flex items-center px-4 py-3 rounded-xl hover:bg-neutral-50 dark:hover:bg-neutral-800 text-[10px] font-black uppercase tracking-widest text-neutral-500">{{ __('Profile') }}</a>
                            <form method="POST" action="{{ route('fabric.logout') }}">
                                @csrf
                                <button type="submit" class="w-full text-left px-4 py-3 rounded-xl hover:bg-rose-50 text-rose-500 text-[10px] font-black uppercase tracking-widest">{{ __('Logout') }}</button>
                            </form>
                        </div>
                    </div>
                    @endauth
                </div>
            </div>
        </nav>

        <!-- Page Content -->
        <main class="flex-1 p-6 sm:p-12">
            <div class="max-w-7xl mx-auto">
                {{ $slot }}
            </div>
        </main>
    </div>
    @livewireScripts
</body>
</html>
