<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ config('app.name', 'Laravel') }} - Preline Minimal</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @livewireStyles
</head>
<body class="bg-white dark:bg-slate-900 min-h-screen">
    <div class="max-w-[50rem] px-4 py-10 sm:px-6 lg:px-8 mx-auto">
        <header class="flex justify-between items-center mb-12">
            <a class="flex-none text-xl font-semibold dark:text-white" href="#">FABRIC</a>
            <nav class="flex items-center gap-5">
                <a class="text-sm font-medium text-gray-500 hover:text-gray-400 dark:text-gray-400" href="#">Dashboard</a>
            </nav>
        </header>

        <main>
            {{ $slot }}
        </main>

        <footer class="mt-20 py-8 border-t border-gray-100 dark:border-gray-800 text-center text-xs text-gray-400">
            Built with Fabric & Preline UI.
        </footer>
    </div>
    @livewireScripts
</body>
</html>
